# Capitalize Every Word Input a string and capitalize the first letter of every word.
def capitalize_every_word(input_str):
    return input_str.title()
input_string = "hello world"
capitalized_string = capitalize_every_word(input_string)
print(capitalized_string)
